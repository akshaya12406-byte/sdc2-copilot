# ── LLM Provider API Keys ──────────────────────────────
# Primary: Google Gemini (via google-genai SDK)
GEMINI_API_KEY=

# Fallback: Groq
GROQ_API_KEY=

# Optional fallback: OpenRouter (deferred for MVP)
OPENROUTER_API_KEY=

# ── LLM Settings ──────────────────────────────────────
# Which provider to use first: gemini | groq | template
LLM_PROVIDER=gemini

# ── App Settings ──────────────────────────────────────
APP_ENV=development
DEFAULT_TIMEZONE=UTC